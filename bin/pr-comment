#!/usr/bin/env bash
set -euo pipefail

if [ -z "${1:-}" ] || [ -z "${2:-}" ]; then
  echo "Usage: $0 <pr-number> <comment-body>" >&2
  exit 1
fi

PR_NUMBER="$1"
BODY="$2"

gh pr comment "$PR_NUMBER" --body "$BODY"
