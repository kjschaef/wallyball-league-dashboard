#!/usr/bin/env bash
set -euo pipefail

if [ -z "${1:-}" ]; then
  echo "Usage: $0 <pr-number>" >&2
  exit 1
fi

PR_NUMBER="$1"

# We ignore error in case the label does not exist on the PR
gh pr edit "$PR_NUMBER" --remove-label "status: ready-to-merge" || true
