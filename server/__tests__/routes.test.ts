/**
 * This test file is skipped because we've migrated from Express to Next.js.
 * The tests have been rewritten in app/__tests__/api/ directory to test the Next.js API routes.
 */

test.skip('Express API Routes (skipped - migrated to Next.js)', () => {
  expect(true).toBe(true);
});
