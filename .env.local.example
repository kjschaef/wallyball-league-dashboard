# Copy this file to `.env.local` for local development. DO NOT commit real secrets.
# Use placeholders here and store real secrets in your environment or in your CI provider.

# Database
# Example: postgresql://user:password@host:port/dbname
DATABASE_URL=postgresql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>/<DB_NAME>?sslmode=require
TEST_DATABASE_URL=postgresql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>/<DB_NAME>?sslmode=require

# Environment
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:5001

# OpenAI / Anthropic / Email - replace with your provider keys in your environment
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
EMAIL_USER=
EMAIL_APP_PASSWORD=

# Notes:
# - Never commit real secrets. Add `.env.local` to your local `.gitignore` (already present).
# - For CI or hosting (Vercel), set environment variables in the platform settings.

